# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'application_meteo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for application_meteo
   pod 'SDWebImage/WebP'

  target 'application_meteoTests' do
    inherit! :search_paths
    # Pods for testing
     pod 'SDWebImage/WebP'
  end

  target 'application_meteoUITests' do
    inherit! :search_paths
    # Pods for testing
     pod 'SDWebImage/WebP'
  end

end
